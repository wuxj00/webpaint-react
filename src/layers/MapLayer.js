import LayerBase from './LayerBase';

export default class MapLayer extends LayerBase{
    constructor( param) {
        super(param);
        console.log(param);
    }
    toPixel() {

    }
    getProjection(){

    }
}

